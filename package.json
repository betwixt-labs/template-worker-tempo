{
	"name": "template-worker-tempo",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy src/index.ts",
		"dev": "wrangler dev src/index.ts",
     "build:schemas": "bebopc build",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^3.0.0",
		"vitest": "^0.31.0",
		"wrangler": "^3.0.0",
		"bebop-tools": "^3.0.5"
	},
	"dependencies": {
		"bebop": "^3.0.4",
		"@tempojs/cloudflare-worker-router": "^0.0.12"
	}
}
